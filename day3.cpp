#include <iostream>
using namespace std;
int main()
{
    // int x, y;
    // cout << "Enter value of x and y";
    // cin >> x >> y;
    // float  = (x + y) / 2;
    // cout << "Average of numbers is " << z;

    // int x, y, square, cube;
    // cout << "Enter value of x ";
    // cin >> x;
    // square = x * x;
    // cube = x * x * x;
    // cout << "Square of x is " << square << endl;
    // cout << "Cube of x is " << cube;

    // float radius, area;
    // cout<<"Enter radius of the circle";
    // cin>>radius;
    // const float pi=3.14;
    // area= pi*radius*radius;
    // cout<<"Area of the circle is "<<area;

    // float radius, hieght;
    // cout << "Enter radius and hieght if cylinder";
    // cin>>radius>>hieght;
    // cout << "Area of cylinder is :" << 3.14 * radius * radius * hieght;

    // int Farhn;
    //     cout<<"Enter temperature in fahrenheit";
    //     cin>>F;
    //     cout<<"Temperature in celsius is "<<(Farhn-32)*5/9;

    // int km;
    // cout<<"Enter distance in km ";
    // cin>>km;
    // cout<<"Distance in meters is "<<km*1000<<endl;
    // cout<<"Distance in centi meters is "<<km*100000<<endl;
    // cout<<"Distance in meters is "<<km*39370.1;

    // int num1, num2;
    // cout<<"Enter value of num1 and num2";
    // cin>>num1>>num2;
    // num1=num1*num2;
    // num2=num1/num2;
    // num1=num1/num2;
    // cout<<num1<<endl<<num2;

    // int user= 17;
    // int vote=18;
    // cout<<((user>= vote)?"yes":"no");

    // int x = 11, y = 7, z = 3;
    // bool a, b;
    // a = x > y && x > z;
    // b = y > z;
    // cout << ((a == 1) ? "x is greater" : ((b == 1) ? "y is greater" : "z is greater"));

    // int a = 3, b = 1, c = 25;
    // cout << ((a < b && a < c) ? "a is smaller" : ((b < c) ? "b is smaller" : "c is smaller"));

    // bool x, y;
    // x=true;
    // y=true;
    // cout<<((x||y));

    // int age=2;
    // bool indian=false;
    // cout<<((age>=18||indian==1)?"You can vote":"you can't vote");

    // int x=7;
    // cout<<((x%2)?"It is odd":"it is even");

    // int x=-0;
    // cout<<((x==0)?"The number is zero":((x>0)?"It is positive":"it is negative"));

    // int x=15;
    // cout<<((x>=80)?"A grade":(60<=x&&x<80)?"b grade":(40<=x&&x<60)?"c grade":(20<=x&&x<40)?"d grade":"e grade");

    // int x=95;
    // cout<<((x<20)?"e grade":(x<40)?"b grade":(x<60)?"c grade":(x<80)?"b grade":"a grade");

    // char ch = ' ';
    // cout << ((ch == 97 || ch == 101 || ch == 105 || ch == 111 || ch == 117) ? "vowel" : "not a vowel");

    // float price=112;
    // cout<<((price>=100)?(price/10)*8:(price/10)*9);

    // bool loggedin;
    // cout<<((loggedin)?"welcome":"please log in");
    // cout<<loggedin;
    return 0;
}

//